# 1. מפתחות סודיים (הכי חשוב!)
.env

# 2. סביבת עבודה וספריות (אלפי קבצים מיותרים)
.venv
venv/
env/
__pycache__/
*.pyc

# 3. קבצי ניהול חבילות של uv (הם נחוצים אבל ה-lock מספיק)
# אם את רוצה לשמור על הריפו נקי, אפשר להשאיר את pyproject.toml ו-uv.lock בחוץ, 
# אבל נהוג להשאיר אותם בגיט. בואי נשאיר אותם בחוץ רק אם הם מפריעים.

# 4. קבצי אקסל של בדיקות (אם את לא רוצה שהם יהיו ציבוריים)
*.xlsx
*.csv

# 5. הגדרות מערכת וגרסאות
.python-version
.DS_Store
Thumbs.db